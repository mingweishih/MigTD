[package]
name = "policy"
version = "0.1.0"
license = "BSD-2-Clause-Patent"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cc-measurement = { path = "../../deps/td-shim/cc-measurement"}
corim = { version = "0.1.3", default-features = false, features = ["profile-intel"], optional = true }
crypto = { path = "../crypto" }
log = { version = "0.4.13", optional = true }
serde = { version = "1.0", default-features = false, features = ["derive", "alloc"]}
serde_json = { version = "1.0", default-features = false, features = ["alloc", "raw_value"] }
td-shim = { path = "../../deps/td-shim/td-shim", default-features = false }
td-shim-interface = { path = "../../deps/td-shim/td-shim-interface"}

[features]
std = []
AzCVMEmu = []
policy_v2 = []
# Prototype: enable CoRIM-based servtd collateral provider alongside the legacy
# TdIdentity + TdTcbMapping JSON format. Decode + match only; signing/verification
# is performed by the existing crypto crate (ECDSA P-384 over the CoRIM TBS).
servtd_corim = ["dep:corim"]
